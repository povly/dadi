doctype html
block vars
html(lang="ru-RU")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title #{title}
        block styles
    body.relative.font-manrope.text-c1.bg-c2.text-normal
        block header
        block content
        block footer
        block scripts